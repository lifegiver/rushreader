<%= form_for(@setting) do |f| %>
  <% if @setting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting.errors.count, "error") %> prohibited this setting from being saved:</h2>

      <ul>
      <% @setting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
		<%= f.label 'Number of articles you want to add' %>
		<%= f.select(:articles_quantity, [['5', 0], ['10', 1], ['20', 2]]) %>
  </div>
	<div class="field">
		<%= f.label 'Interval between readings your articles (in minutes)' %>
		<%= f.select(:interval_between_readings, [['25', 0], ['45', 1], ['60', 2]]) %>
  </div>
	<div class="field">
		<%= f.label 'Time for reading of each article (in minutes)'%>
		<%= f.select(:time_for_reading, [['5', 0], ['10', 1], ['15', 2]]) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

